<?xml version="1.0" encoding="utf-8"?>
<package unique-identifier="uid" xmlns:opf="http://www.idpf.org/2007/opf" xmlns:asd="http://www.idpf.org/asdfaf">
    <metadata>
        <dc-metadata  xmlns:dc="http://purl.org/metadata/dublin_core" xmlns:oebpackage="http://openebook.org/namespaces/oeb-package/1.0/">
            <dc:Title>{{ title }}</dc:Title>
            <dc:Language>zh</dc:Language>
            <dc:Creator>{{ author }}</dc:Creator>
            <dc:Copyrights>Copyright owner</dc:Copyrights>
            <dc:Publisher>{{ publisher }}</dc:Publisher>
            <x-metadata>
                <EmbeddedCover>{{ coverimage }}</EmbeddedCover>
            </x-metadata>
        </dc-metadata>
    </metadata>
    <manifest>
        {% for item in manifest %}
        <item id="{{ item.id }}" media-type="{{ item.mediatype }}" href="{{ item.href }}"></item>
        {% endfor %}
    </manifest>
    <spine toc="ncx">
        {% for item in ncx %}
        <itemref idref="item.id"/>
        {% endfor %}
    </spine>
    <guide>
        <!-- <reference type="toc" title="Table of Contents" href="toc.html"/>
        <reference type="text" title="Book" href="index.html"/> -->
        {% for item in guide %}
        <reference type="{{ item.type }}" title="{{ item.title }}" href="{{ item.href }}"/>
        {% endfor %}
    </guide>
</package>